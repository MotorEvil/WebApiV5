@if (Request.IsAuthenticated)
{
    using (Html.BeginForm("Logout", "RegisterAndLogin", FormMethod.Post, new { Id = "logoutForm", @class = "nav-link" }))
    {
        @Html.AntiForgeryToken()
        <ul class="navbar-nav mr-auto">
            <li class="nav-item">@HttpContext.Current.User.Identity.Name</li>
            <li class="nav-item"><a href="javascript:document.getElementById('logoutForm').submit()">Atsijungti</a></li>
        </ul>

    }
}
else
{
    <ul class="navbar-nav mr-auto">
        <li class="nav-item">@Html.ActionLink("Registruotis", "Registration", "RegisterAndLogin", routeValues: null, htmlAttributes: new { id = "registerLink", @class = "nav-link" })</li>
        <li class="nav-item">@Html.ActionLink("Prisijungti", "Login", "RegisterAndLogin", routeValues: null, htmlAttributes: new { id = "loginLink", @class = "nav-link" })</li>
    </ul>
}

